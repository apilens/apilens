# Local state shouldn't be committed — we use a GCS backend.
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# tfvars contains environment-specific config + sometimes secrets.
terraform.tfvars
*.auto.tfvars

# Plan files are local artifacts of `terraform plan -out`.
*.tfplan

# Editor noise
*.swp
